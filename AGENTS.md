# DarkSeries 작업 지침

## 기본 원칙

- 작업을 시작하기 전에 기존 변경사항과 관련 문서를 확인한다.
- 사용자가 요청한 범위 안에서만 파일을 수정한다.
- 기존에 작성 중인 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- 분석, 설명, 검토 요청에서는 명시적인 수정 요청이 없는 한 코드를 변경하지 않는다.
- 작업 의도가 불분명하고 결과가 크게 달라질 수 있다면 수정 전에 사용자에게 확인한다.

## 프로젝트 문서

- 코드 구조를 파악할 때 `DarkSeries/Docs/CodeAnalysis.md`를 먼저 참고한다.
- 이슈, 브랜치, 커밋, 병합 작업 전에는 `DarkSeries/Docs/GitWorkflow.md`를 확인한다.
- 문서와 실제 코드가 다르면 실제 코드를 기준으로 판단하고 차이를 알린다.

## Git 작업 원칙

- 기능 작업은 연결된 이슈와 이슈 브랜치에서 진행한다.
- 사용자의 명시적인 요청 없이 브랜치 생성, 커밋, 푸시, 병합을 수행하지 않는다.
- 모든 이슈 브랜치는 `Dev` 브랜치로 병합한다.
- `Dev`를 `main`에 병합하려면 사용자의 명시적인 요청을 받은 뒤 병합 직전에 다시 확인한다.
- 커밋에는 요청 범위와 관계없는 변경사항을 포함하지 않는다.

## Unity 프로젝트 주의 사항

- Unity가 자동 생성한 코드는 직접 수정하지 않는다.
- 씬, Prefab, Animator Controller, Animation Clip, Input Actions는 요청 범위에 포함된 경우에만 수정한다.
- `.meta` 파일을 임의로 삭제하거나 GUID를 변경하지 않는다.
- Unity 직렬화 필드의 이름이나 타입을 변경할 때는 기존 에셋 데이터에 미치는 영향을 확인한다.

## 변경 및 검증

- 수정 전후로 Git 변경사항을 확인해 사용자 변경과 작업 변경을 구분한다.
- 가능한 범위에서 컴파일 오류, 테스트, 정적 검사 또는 관련 로그를 확인한다.
- Unity Editor에서만 검증할 수 있는 사항은 검증하지 못한 부분을 결과에 명시한다.
- 작업 완료 시 변경한 파일과 핵심 내용을 간단히 보고한다.

## 코딩 컨벤션

- 변수명 작성 시 Camel Case 사용
- 함수명은 대문자로 시작 (예시 : Move, GetNearTarget)
